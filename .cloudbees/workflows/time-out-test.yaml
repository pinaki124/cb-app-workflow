apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow

on:
  workflow_dispatch:

jobs:
  build:
    timeout-minutes: 2
    steps:
      - name: Say hello
        timeout-minutes: 1
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world - ${{ vars.vars_1 }}"
          sleep 90
  
  
